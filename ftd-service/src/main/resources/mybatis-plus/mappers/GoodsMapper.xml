<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ftd.seckill.ftdservice.mapper.GoodsMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.ftd.seckill.ftdservice.entity.Goods">
        <id column="id" property="id" />
        <result column="goods_name" property="goodsName" />
        <result column="goods_title" property="goodsTitle" />
        <result column="goods_imp" property="goodsImp" />
        <result column="goods_detail" property="goodsDetail" />
        <result column="goods_price" property="goodsPrice" />
        <result column="goods_stock" property="goodsStock" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, goods_name, goods_title, goods_imp, goods_detail, goods_price, goods_stock
    </sql>
    
    <select id="findGoodsVo" resultType="com.ftd.seckill.ftdservice.vo.GoodsVo">
        SELECT
            g.id,
            g.goods_name,
            g.goods_imp,
            g.goods_detail,
            g.goods_price,
            g.goods_stock,
            sg.seckill_price,
            sg.stock_count,
            sg.start_date,
            sg.end_date
        FROM
            goods g
                LEFT JOIN seckill_goods AS sg ON g.id = sg.good_id
    </select>

    <select id="findGoodsVoByGoodsId" resultType="com.ftd.seckill.ftdservice.vo.GoodsVo">
        SELECT
            g.id,
            g.goods_name,
            g.goods_imp,
            g.goods_detail,
            g.goods_price,
            g.goods_stock,
            sg.seckill_price,
            sg.stock_count,
            sg.start_date,
            sg.end_date
        FROM
            goods g
                LEFT JOIN seckill_goods AS sg ON g.id = sg.good_id
        WHERE
            g.id = #{goodsId}
    </select>

</mapper>
